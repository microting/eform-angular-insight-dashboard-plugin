// import loginPage from '../../../Login.page';
// import pluginPage from '../../../Plugin.page';

describe('Application settings page - site header section', () => {
  // beforeEach(() => {
  //   cy.visit('http://localhost:4200');
  //   loginPage.login();
  //   pluginPage.Navbar.goToPluginsPage();
  // });

  it('should activate the plugin', () => {
    assert.isTrue(true);
    // const pluginName = 'Microting InSight Dashboard Plugin';

    // cy.contains('.mat-mdc-row', pluginName).first().find('#actionMenu').click();
    // cy.wait(500);

    // cy.get('#plugin-status-button0').scrollIntoView().should('be.visible').click();
    // cy.wait(500);

    // cy.get('body').then(($body) => {
    //   if ($body.find('#pluginOKBtn').length > 0) {
    //     cy.get('#pluginOKBtn').click();
    //     cy.wait(100000);
    //   }
    // });

    // cy.visit('http://localhost:4200');
    // loginPage.login();
    // pluginPage.Navbar.goToPluginsPage();

    // cy.contains('.mat-mdc-row', pluginName).first().find('#actionMenu').click();
    // cy.wait(500);

    // cy.get('#plugin-status-button0')
    //   .find('mat-icon')
    //   .should('contain.text', 'toggle_on');
  });
});
